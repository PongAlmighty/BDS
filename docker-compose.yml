services:
  bds:
    build: .
    container_name: bds
    ports:
      - "18080:18080"
      - "18765:18765"
    volumes:
      - ./tokens.json:/app/tokens.json
      - ./.env:/app/.env
    restart: unless-stopped
